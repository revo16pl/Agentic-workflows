# Execution

Store deterministic Python scripts here.

Guidelines:
- Prefer scripts over manual/LLM-only operations for repeatable logic.
- Read required inputs from args/env.
- Keep scripts idempotent when possible.
- Write temporary/intermediate files to `.tmp/`.
